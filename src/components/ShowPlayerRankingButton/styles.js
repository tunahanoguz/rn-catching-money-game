import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    button: {
        alignSelf: 'stretch',
        padding: 30,
        backgroundColor: 'red',
        borderRadius: 20,
    },
    buttonText: {
        color: 'white',
        fontSize: 16,
        fontWeight: '600',
    },
});

export default styles;
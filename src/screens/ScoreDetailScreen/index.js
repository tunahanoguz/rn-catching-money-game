import React from 'react';
// import { Text } from 'react-native';
import { ScreenContainer, ScreenTitle } from '../../components';

function ScoreDetailScreen() {
    return (
        <ScreenContainer>
            <ScreenTitle>Score Detail</ScreenTitle>
        </ScreenContainer>
    );
}

export default ScoreDetailScreen;